services:
  - type: web
    name: rappel-conso
    runtime: python
    region: frankfurt
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn server-rappel:mcp.app --host 0.0.0.0 --port $PORT"
